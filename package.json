{

  "name": "@mojsoski/ayanami",
  "version": "1.0.0",
  "description": "Workload splitting library for asynchronous workloads",
  "main": "dist/lib/index.js",
  "types": "dist/lib/index.d.ts",
  "keywords": ["async", "task", "scheduler", "worker"],
  "author": "Mihail Mojsoski",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "scripts": {
    "watch": "npx tsc --watch",
    "build": "npx tsc",
    "prepare" : "npm run build",
    "version" : "git add -A lib",
    "postversion" : "git push && git push --tags",
    "start": "node dist/example.js"
  },
  "publishConfig": {
    "access": "public"
  }

}
